check_id,description,status,severity,notes
CC-01,3 HTTP + 2 internal handlers,PASS,,router.go: /create (internalMW) + /simulate (jwtMW) + /callback. Internal: ProcessCallback + ExpirySweeper. outbox_relay.go absent (see F-01).
CC-02,7-step process_callback ordering — amount-check BEFORE terminal-state guard,PASS,,process_callback.go Step 2 (lines 65-81) fires before Step 5 (line 109). Ordering confirmed in both production code and test wrapper.
CC-03,HMAC-SHA256 + constant-time compare,PASS,,signature.go uses crypto/hmac+sha256+subtle.ConstantTimeCompare. algo pinned. rawBody read before JSON parse. internalSecretMW also uses ConstantTimeCompare.
CC-04,Sweeper 60s default — no event emission,PASS,,config.go SWEEPER_CADENCE_SECONDS envDefault:60. expiry_sweeper.go tick() calls only MarkExpired; zero outbox or dedup writes.
CC-05,events.payment.expired NOT emitted in MVP,PASS,,process_callback.go EXPIRED branch gated by emitExpiredEvent flag (default false per config). Test TestProcessCallback_ExpiredOutcome_NoOutboxInMVP confirms 0 outbox rows.
CC-06,Dedup key = sha256(intent_id || provider_status),PASS,,computeDedupKey: sha256.Sum256(intentID.String() + "|" + providerStatus). Server fields only. 64-char hex confirmed in TestProcessCallback_DedupKeyComposition.
CC-07,14 tests per Dev report,PASS,,10 Test* funcs; 9 executable (1 compile guard excluded). 2 table-driven: AmountMismatch (2 sub) + HMAC (5 sub). Total runnable = 14.
F-01,outbox_relay.go absent — Kafka drain missing,FINDING,LOW,OutboxStorage interface defined but relay goroutine not implemented. main.go starts sweeper only. payment.completed/failed events accumulate without drain.
F-02,simulate timeout outcome returns 422 instead of delegating to process_callback EXPIRED path,FINDING,LOW,handler_simulate.go lines 89-97 refuse timeout with 422. Spec requires timeout->EXPIRED->process_callback. simulate-EXPIRED scenario not reachable via HTTP.
