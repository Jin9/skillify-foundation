# 02 — Evidence: q[N]

**Sub-question**: [restate the sub-question verbatim]
**Searched on**: [ISO timestamp]

## Findings

Each finding cites a specific file path with line numbers, or a URL. Quote the relevant lines verbatim. Do not paraphrase the source.

### F1: [short finding label]

**Source**: `path/to/file.ext:42-58`

```
[verbatim quoted lines from the source]
```

**What this tells us**: [one to three sentences explaining the finding's relevance]

### F2: [short finding label]

**Source**: `path/to/other.ext:12`

```
[verbatim quoted lines]
```

**What this tells us**: [explanation]

## Searches performed

List the queries / paths you searched, even those that returned nothing. This makes the gather step reproducible.

- `grep -r "pattern" path/` → [N hits | 0 hits]
- Read `path/to/file.ext` → [summary]
- Web fetch `https://...` → [summary]

## Gaps

Things you tried to find but couldn't. State what you searched for so the analysis phase knows what's missing.

- [gap 1]

## No-evidence case

If no evidence was found at all, state explicitly: **"No evidence found for q[N]."** Then list what you searched for and why it wasn't there. Do not fabricate findings.
