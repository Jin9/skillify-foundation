# Workflow routine registry

One file per routine, launched by name via the launching-workflow-routines skill. Validate every file here with that skill's scripts/validate_routine.py before adding it to this table.

| routine | summary | nodes | version | file |
|---------|---------|-------|---------|------|
| {routine-name} | {summary copied from frontmatter} | {n} | {version} | {routine-name}.md |
